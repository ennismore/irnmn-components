const prefix = 'irnmn-rooms-selector';

export const CLASS_NAMES = {
    roomsSelector: `${prefix}-rooms-selector`,
    roomCountSelect: `${prefix}-room-count-select`,
    roomContainer: `${prefix}-room-container`,
};